import { Layout } from 'antd';

export default function PageFooter(){
    const { Footer } = Layout;
    return(
        <Footer style={{ textAlign: 'center', }}>
            ©2023 週末は休みの日
        </Footer>
    )
}